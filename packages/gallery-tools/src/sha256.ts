export function sha256(input: string): string {
  // Stub implementation.  A full implementation would compute the SHA‑256
  // hash of the input string.  This stub returns an empty string.
  return '';
}