export function buildPackage(): void {
  // Stub implementation.  In a full implementation this function would
  // assemble the persona package into a zip archive.  Currently it
  // prints a message to indicate where build logic would go.
  console.log('Building persona package (stub)');
}